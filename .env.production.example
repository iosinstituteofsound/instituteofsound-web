# instituteofsound-web — PRODUCTION (Vercel import)
# cp .env.production.example .env.production

VITE_SITE_URL=https://www.instituteofsound.in
VITE_API_BASE_URL=https://instituteofsound-api.onrender.com
VITE_USE_V1_API=true

# Socket.io realtime (connects directly to API host in production)
VITE_WS_ENABLED=true
# VITE_WS_URL=  # optional — auto-derived from VITE_API_BASE_URL

# ---------- Cloudinary (browser uploads) ----------
VITE_CLOUDINARY_CLOUD_NAME=your_cloud_name
VITE_CLOUDINARY_UPLOAD_PRESET=ios_unsigned

# ---------- YouTube (artist channel import) ----------
VITE_YOUTUBE_API_KEY=
